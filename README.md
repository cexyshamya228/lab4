Лабораторная работа №4

Задание 1 — List

Задача:
Проверка списка на наличие одинаковых элементов

Реализация:
Создан универсальный метод, использующий HashSet, который автоматически отслеживает повторяющиеся элементы.

Методы класса:

HasDuplicates<T>(List<T> list) — поиск дубликатов.

TryParse<T>(string input, out T value) — безопасное преобразование типов.

TestTask1() — запуск демонстрации.

тест:

Тестовый список: 1, 2, 3, 4, 2
Есть ли дубликаты: True

Задание 2 — LinkedList

Задача:
Удалить первое вхождение заданного элемента из списка.

Реализация:
Используется LinkedList<object>, поиск осуществляется через метод Find.

Основной метод:

TestTask2()

тест:

Исходный список: 1 → 2 → 3 → 2
Введите элемент для удаления: 2
Итоговый список: 1 → 3 → 2

Задание 3 — HashSet

Задача:
Определить, какие песни нравятся всем, некоторым или никому из слушателей.

Реализация:
Создан класс Meloman, используется HashSet<string> для хранения любимых треков.

Методы:

AnalyzeMusicPreferences(...)

TestTask3()

Тест:

Song1 — нравится некоторым  
Song2 — нравится всем  
Song3 — не нравится никому

Задание 4 — HashSet + файл

Задача:
Вывести все гласные буквы русского алфавита, встречающиеся не более чем в одном слове.

Реализация:
Чтение текста из файла, анализ слов, подсчёт встречаемости.

Метод:

TestTask4()

тест:

Файл содержит: "мама мыла раму"  
Результат: ы у

Задание 5 — Dictionary / файл

Задача:
Определить лучшего участника, который стал победителем олимпиады, либо их количество.

Реализация:

Создан класс:

Participant
{
    LastName
    FirstName
    Class
    Score
}


Используется:

запись данных в файл (CreateInputFile)

чтение из файла

Считывание данных всех участников

Формирование списка участников с максимальным баллом.

Основные методы:

CreateInputFile(string path)

TestTask5()

тест:

Один лучший участник:

Семенов Егор


Несколько участников:

3



Структура проекта

Program.cs — меню и взаимодействие с пользователем

Methods.cs — логика всех заданий (1–5)
